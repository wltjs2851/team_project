<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.kh.team.mappers.kcal">
	<!-- 	운동 Kcal 사전 -->
	<!-- 글 추가 -->
	<insert id="insertKcal">
		insert into tbl_kcal (kno, k_name, k_time, kcal, k_dgree)
		values (seq_kcal_kno.nextval, #{k_name}, #{k_time}, #{kcal}, #{k_dgree})
	</insert>
	
	<!-- 글 수정 -->
	<update id="updateKcal">
		update tbl_kcal set
			k_name = #{k_name},
			k_time = #{k_time},
			kcal = #{kcal},
			k_dgree = #{k_dgree}
		where kno = #{kno}
	</update>
	
	<!-- 글 삭제 -->
	<delete id="deleteKcal">
		delete from tbl_kcal
		where kno = #{kno}
	</delete>
	
	<!-- 글 상세조회 -->
	<select id="selectByKno" resultType="KcalVo">
		select * from tbl_kcal
		where kno = #{kno}
	</select>
	
	<!-- 글 목록조회 -->
	<select id="listKcal" resultType="KcalVo">
		select * from tbl_kcal
		order by kno desc
	</select>
	
	
</mapper>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.kh.team.mappers.group">
	<select id="getNextGno" resultType="int">
		select seq_group_gno.nextval from dual 
	</select>

	<insert id="insertGroup">
		insert into tbl_group(gno, g_name, excercise_type, g_attend, g_intro, g_location, g_pic, g_leader)
			values(#{gno}, #{g_name}, #{excercise_type}, #{g_attend},#{g_intro}, #{g_location}, #{g_pic, jdbcType=VARCHAR}, #{g_leader})
	</insert>
	
	<select id="selectGroup" resultType="GroupVo">
		select * from tbl_group order by gno desc
	</select>
	
	<select id="groupByGno" resultType="GroupVo">
		select * from tbl_group where gno = #{gno}
	</select>
	
	<update id="updateGroup">
		update tbl_group set g_name = #{g_name}, excercise_type = #{excercise_type}, g_attend = #{g_attend},
				g_intro = #{g_intro}, g_location = #{g_location}, g_pic = #{g_pic, jdbcType=VARCHAR}
			where gno = #{gno}
	</update>
	
	<insert id="insertJoinGroup">
		insert into tbl_group_join(jno, gno, userid)
			values(seq_group_jno.nextval, #{gno}, #{userid})
	</insert>
	
	<update id="updateGroupMember">
		update tbl_group set g_present = #{g_present} + 1 where gno = #{gno}
	</update>

	
	<delete id="deleteGroup">
		delete from tbl_group where gno = #{gno}
	</delete>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.kh.team.dao.ScheduleDao">

	<insert id="schedule_add">
		insert into tbl_schedule
			(schno, sch_date, sch_content)
		values
			(#{schno}, #{sch_date}, #{sch_content})
	</insert>

	
   	
	<select id="before_schedule_add_search" resultType="int">
		
		select count(*) from tbl_schedule 
		where sch_date = #{sch_date}
		
	</select>
	
   	<select id="schedule_list" resultType="ScheduleDto">
		
		select * from tbl_schedule 
		where sch_date &gt;= #{db_startDate} and sch_date &lt;= #{db_endDate} 
		order by sch_date, schno
		
	</select>
</mapper>